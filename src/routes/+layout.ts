// Disable server-side rendering for the entire application.
// This way all requests are handled on the client side, and
// we don't risk upsetting any API with rate limiting issues.
export const ssr = false;
